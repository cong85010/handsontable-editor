import { TableConfig } from './types';

export const TABLE_CONFIG: TableConfig = {
  height: 800,
  licenseKey: 'non-commercial-and-evaluation',
};

export const OPTIONS_ADD_NEW_PREFIX = 'ADD_NEW_';

